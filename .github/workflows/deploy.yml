name: build-and-deploy
on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install deps
        working-directory: frontend
        run: npm ci

      - name: Build
        working-directory: frontend
        run: npm run build

      # Se o build gerar "dist" (Vite):
      - name: Deploy (Vite - dist)
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          protocol: ftps
          port: 21
          server-dir: public_html/3dstuff.com.br/
          local-dir: frontend/dist
          exclude: |
            **/.git*
            **/.github/**
            **/node_modules/**

      # Se for CRA e o build gerar "build", troque o passo acima por este:
      # - name: Deploy (CRA - build)
      #   uses: SamKirkland/FTP-Deploy-Action@v4.3.5
      #   with:
      #     server: ${{ secrets.FTP_SERVER }}
      #     username: ${{ secrets.FTP_USERNAME }}
      #     password: ${{ secrets.FTP_PASSWORD }}
      #     protocol: ftps
      #     port: 21
      #     server-dir: public_html/3dstuff.com.br/
      #     local-dir: frontend/build
      #     exclude: |
      #       **/.git*
      #       **/.github/**
      #       **/node_modules/**
